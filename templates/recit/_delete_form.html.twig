<form method="post" action="{{ path('recit_delete', {'id': recit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recit.id) }}">
    <button class="btn btn-dark">Supprimer</button>
</form>
