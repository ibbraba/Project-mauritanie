{% extends 'base.html.twig' %}

{% block content %}
<div style="text-align: center; color: var(--secondary)">
<h2>Récits</h2>
  <p> Les récits sont des histoires de notre mémoire que nous vous partageons avec le coeur. Chaque personne a une histoire
  à vous raconter et nous esperons que nos récits vous feront découvrir plein de nouvelles choses. Découvrez tous nos récits
  dans cette rubrique
  </p>
</div>


  <div class="recits-list">
    {% for recit in list %}

      <div class="card w-50">
        <div class="card-body">
          <h5 class="card-title">{{ recit.titre }}</h5>
          <p class="card-text">{{ recit.description }}</p>
          <a href="{{ path("recit_show", {id:recit.id} ) }}" class="btn btn-warning">Lire le récit</a>
        </div>
      </div>


    {% endfor%}


  </div>
{% endblock %}